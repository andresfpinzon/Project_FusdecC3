@model IEnumerable<FusdecMvc.Models.School>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SchoolName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SchoolEmail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SchoolName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SchoolEmail)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSchool">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdSchool">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSchool">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
